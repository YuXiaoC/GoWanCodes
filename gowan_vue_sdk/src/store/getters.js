const getters = {
  gowanComponentName: state => state.GowanComponent.gowanComponentName,
  floatBall: state => state.GowanComponent.floatBall,
  notice: state => state.GowanComponent.notice,
  rechargeAges: state => state.GowanComponent.rechargeAges,
  slideBar: state => state.GowanComponent.slideBar
}

export default getters
